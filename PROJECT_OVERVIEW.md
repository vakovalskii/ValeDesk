# ValeDesk: Полное описание проекта

> **ValeDesk** — Very Almost Local, Eventually Reasonable Assistant  
> Настольный AI-ассистент с поддержкой локальных моделей

**Версия:** 0.0.8  
**Автор:** Valerii Kovalskii  
**Репозиторий:** https://github.com/vakovalskii/ValeDesk

---

## 📋 Оглавление

1. [Философия проекта](#-философия-проекта)
2. [Архитектура](#-архитектура)
3. [Технологический стек](#-технологический-стек)
4. [Структура проекта](#-структура-проекта)
5. [Инструменты (Tools)](#-инструменты-tools)
6. [Система памяти](#-система-памяти)
7. [Scheduler](#-scheduler)
8. [Skills Marketplace](#-skills-marketplace)
9. [Безопасность](#-безопасность)
10. [Хранение данных](#-хранение-данных)
11. [Сборка и разработка](#-сборка-и-разработка)
12. [Лицензия](#-лицензия)

---

## 🎯 Философия проекта

### Проблема
Современные AI-ассистенты (ChatGPT, Claude) — это облачные сервисы с закрытым кодом. Пользователь не контролирует:
- Куда отправляются его данные
- Как обрабатываются запросы
- Какие ограничения накладываются

### Решение ValeDesk
**Полный контроль пользователя** через:

| Принцип | Реализация |
|---------|------------|
| **Приватность** | Все данные хранятся локально в SQLite |
| **Прозрачность** | Open-source код, логирование каждого запроса |
| **Гибкость** | Работа с любым OpenAI-совместимым API |
| **Расширяемость** | Легко добавлять новые инструменты |

### Название
**V**ery **A**lmost **L**ocal, **E**ventually Reasonable Assistant:
- "Almost Local" — система стремится к локальности, но может использовать внешние API
- "Eventually Reasonable" — ирония над тем, что AI не всегда выдаёт идеальные ответы с первой попытки

---

## 🏗 Архитектура

### Обзор
ValeDesk использует **трёхслойную архитектуру**:

```
┌─────────────────────────────────────────────────────────────┐
│                    Tauri App (Rust)                         │
│  ┌─────────────┐    ┌──────────────┐    ┌───────────────┐   │
│  │  main.rs    │───▶│  SQLite DB   │    │   Scheduler   │   │
│  │  (IPC hub)  │    │  sessions.db │    │   Service     │   │
│  └─────────────┘    └──────────────┘    └───────────────┘   │
│         │                                       │           │
│         │ JSON Events                          │ stdin/out  │
│         ▼                                       ▼           │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              Node.js Sidecar (pkg binary)           │    │
│  │  ┌──────────────┐  ┌───────────┐  ┌─────────────┐   │    │
│  │  │ LLM Runner   │  │  Tools    │  │  Session    │   │    │
│  │  │ (OpenAI SDK) │  │ Executor  │  │  Store      │   │    │
│  │  └──────────────┘  └───────────┘  └─────────────┘   │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                            ▲
                            │ WebView
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    React UI (Vite)                          │
│  ┌───────────────┐  ┌────────────┐  ┌──────────────┐        │
│  │  useAppStore  │  │ Components │  │  Tauri IPC   │        │
│  │  (Zustand)    │  │            │  │  Bridge      │        │
│  └───────────────┘  └────────────┘  └──────────────┘        │
└─────────────────────────────────────────────────────────────┘
```

### Слой 1: Tauri (Rust)

**Файлы:** `src-tauri/src/`

| Модуль | Назначение |
|--------|------------|
| `main.rs` | Точка входа, IPC-маршрутизация, управление sidecar |
| `db.rs` | SQLite операции (сессии, сообщения, настройки) |
| `scheduler.rs` | Планировщик задач с нативными уведомлениями |
| `sandbox.rs` | Изолированное выполнение JS/Python |

**Преимущества Tauri над Electron:**
- Размер приложения: ~10MB vs ~150MB
- Потребление RAM: значительно меньше
- Безопасность: Rust исключает целые классы уязвимостей

### Слой 2: Node.js Sidecar

**Файлы:** `src/sidecar/`, `src/agent/`

Sidecar — отдельный Node.js процесс, скомпилированный в бинарник с помощью `pkg`. Общается с Rust через stdin/stdout в формате JSON.

**Ключевые модули:**

| Файл | Назначение |
|------|------------|
| `main.ts` | Точка входа sidecar, обработка IPC |
| `libs/runner-openai.ts` | LLM-loop, streaming, retry-логика |
| `libs/tools-executor.ts` | Выполнение инструментов |
| `libs/session-store.ts` | Хранение состояния сессий |
| `libs/tools/` | Реализации всех инструментов |

**Почему отдельный процесс?**
- Изоляция сложной LLM-логики от UI
- Независимая отладка
- Использование npm-экосистемы

### Слой 3: React UI

**Файлы:** `src/ui/`

| Компонент | Назначение |
|-----------|------------|
| `App.tsx` | Корневой компонент |
| `store/useAppStore.ts` | Zustand store для состояния |
| `components/` | UI-компоненты (Sidebar, SettingsModal, TodoPanel...) |
| `platform/` | Абстракция над Tauri/Electron/Web |

---

## 🛠 Технологический стек

### Backend (Rust)

| Технология | Назначение |
|------------|------------|
| **Tauri 2.x** | Desktop framework |
| **SQLite (rusqlite)** | База данных |
| **serde** | Сериализация JSON |
| **chrono** | Работа с датами |
| **tauri-plugin-notification** | Нативные уведомления |

### Sidecar (Node.js)

| Технология | Назначение |
|------------|------------|
| **OpenAI SDK** | Взаимодействие с LLM |
| **Playwright** | Browser automation |
| **pdf-parse** | Извлечение текста из PDF |
| **mammoth** | Извлечение текста из DOCX |
| **QuickJS (WASM)** | Песочница для JavaScript |
| **@tavily/core** | Web search API |
| **pkg** | Компиляция в бинарник |

### Frontend (React)

| Технология | Назначение |
|------------|------------|
| **React 19** | UI-фреймворк |
| **Zustand** | State management |
| **Tailwind CSS** | Стилизация |
| **Vite** | Сборка |
| **react-markdown** | Рендеринг Markdown |
| **highlight.js** | Подсветка кода |
| **mermaid** | Диаграммы |

---

## 📁 Структура проекта

```
ValeDesk/
├── src/
│   ├── agent/                    # LLM-агент и инструменты
│   │   ├── libs/
│   │   │   ├── tools/            # Реализации инструментов
│   │   │   │   ├── read-tool.ts
│   │   │   │   ├── write-tool.ts
│   │   │   │   ├── edit-tool.ts
│   │   │   │   ├── bash-tool.ts
│   │   │   │   ├── execute-python-tool.ts
│   │   │   │   ├── execute-js-tool.ts
│   │   │   │   ├── web-search.ts
│   │   │   │   ├── browser-tool.ts
│   │   │   │   ├── git-tool.ts
│   │   │   │   ├── memory-tool.ts
│   │   │   │   ├── manage-todos-tool.ts
│   │   │   │   ├── schedule-task-tool.ts
│   │   │   │   └── ...
│   │   │   ├── prompts/
│   │   │   │   ├── system.txt    # Системный промпт
│   │   │   │   └── initial_prompt.txt
│   │   │   ├── runner-openai.ts  # LLM loop
│   │   │   ├── tools-executor.ts
│   │   │   ├── tools-definitions.ts
│   │   │   ├── session-store.ts
│   │   │   ├── settings-store.ts
│   │   │   ├── skills-loader.ts
│   │   │   └── ...
│   │   ├── main.ts               # Точка входа агента
│   │   ├── types.ts
│   │   └── git-utils.ts
│   │
│   ├── sidecar/                  # Sidecar entry point
│   │   └── main.ts
│   │
│   └── ui/                       # React UI
│       ├── components/
│       │   ├── Sidebar.tsx
│       │   ├── SettingsModal.tsx
│       │   ├── TodoPanel.tsx
│       │   ├── PromptInput.tsx
│       │   ├── EventCard.tsx
│       │   └── ...
│       ├── store/
│       │   └── useAppStore.ts
│       ├── platform/             # Platform abstraction
│       │   ├── tauri.ts
│       │   ├── electron.ts
│       │   └── web.ts
│       ├── App.tsx
│       └── main.tsx
│
├── src-tauri/                    # Rust backend
│   └── src/
│       ├── main.rs               # Tauri app, IPC handlers
│       ├── db.rs                 # SQLite operations
│       ├── scheduler.rs          # Task scheduler
│       └── sandbox.rs            # Code sandbox
│
├── scripts/                      # Build scripts
│   ├── build_sidecar.cjs
│   ├── dev.cjs
│   └── ...
│
├── package.json
├── Makefile
├── vite.config.ts
├── tsconfig.json
└── electron-builder.json
```

---

## 🔧 Инструменты (Tools)

Все инструменты следуют конвенции `snake_case` с паттерном `verb_noun`.

### Файловые операции

| Инструмент | Описание |
|------------|----------|
| `read_file` | Чтение текстовых файлов (до 5MB) |
| `write_file` | Создание новых файлов |
| `edit_file` | Редактирование (search & replace) |
| `search_files` | Поиск по glob-паттерну (`*.pdf`, `src/**/*.ts`) |
| `search_text` | Grep-like поиск в содержимом файлов |
| `read_document` | Извлечение текста из PDF/DOCX (до 10MB) |
| `attach_image` | Прикрепление изображений для vision-моделей |

### Выполнение кода

| Инструмент | Описание |
|------------|----------|
| `execute_python` ⭐ | Системный Python 3 + все pip-пакеты |
| `execute_js` | QuickJS WASM sandbox (ограниченный) |
| `run_command` | Bash/PowerShell команды |

**Различия песочниц:**

```
┌─────────────────────────────────────────────────────────────┐
│                    execute_python                            │
│  ✅ Полная stdlib (json, os, re, datetime, sqlite3...)      │
│  ✅ Любые pip-пакеты (numpy, pandas, requests, pillow...)   │
│  ✅ Файловый I/O в пределах workspace                       │
│  ✅ Сетевые запросы (requests, httpx)                       │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    execute_js                                │
│  ✅ fs.readFileSync, fs.writeFileSync                       │
│  ✅ JSON, Math, Date, console.log                           │
│  ❌ require(), import — НЕТ npm-модулей                     │
│  ❌ fetch(), async/await — НЕТ сети                         │
│  ❌ setTimeout, setInterval — НЕТ таймеров                  │
└─────────────────────────────────────────────────────────────┘
```

### Web-инструменты

| Инструмент | Описание |
|------------|----------|
| `search_web` | Интернет-поиск (Tavily/Z.AI) |
| `extract_page` | Полное содержимое страницы |
| `read_page` | Z.AI Reader |
| `render_page` | Chromium для SPA/Telegram |
| `fetch_html` | Загрузка HTML |
| `fetch_json` | Загрузка и парсинг JSON |
| `download_file` | Скачивание файлов |

### Browser Automation

| Инструмент | Описание |
|------------|----------|
| `browser_navigate` | Переход по URL |
| `browser_click` | Клик по элементу |
| `browser_type` | Ввод текста |
| `browser_screenshot` | Скриншот страницы |
| `browser_scroll` | Прокрутка |

### Git-операции

| Инструмент | Описание |
|------------|----------|
| `git_status` | Статус репозитория |
| `git_log` | История коммитов |
| `git_diff` | Изменения файлов |
| `git_commit` | Создание коммита |
| `git_push` | Отправка в remote |
| `git_pull` | Получение изменений |
| `git_branch` | Управление ветками |
| `git_checkout` | Переключение веток |

### Управление задачами

| Инструмент | Описание |
|------------|----------|
| `manage_todos` | Визуальный todo-panel с прогрессом |
| `schedule_task` | Создание/управление scheduled tasks |
| `manage_memory` | Персистентные настройки пользователя |
| `load_skill` | Загрузка специализированных инструкций |

---

## 🧠 Система памяти

### Как работает

Память хранится в файле `~/.valera/memory.md` (или `~/Library/Application Support/ValeDesk/memory.md` на macOS).

```markdown
# User Preferences

## Имя
Меня зовут Валерий

## Проекты
- ValeDesk — основной проект, AI-ассистент
- Работаю с Python и TypeScript

## Предпочтения
- Предпочитаю краткие ответы
- Код комментировать на русском
```

### Инструмент manage_memory

```typescript
// Сохранение
manage_memory({ operation: "write", key: "name", value: "Валерий" })

// Чтение
manage_memory({ operation: "read", key: "name" })

// Удаление
manage_memory({ operation: "delete", key: "name" })
```

### Интеграция с LLM

При каждом новом сообщении содержимое `memory.md` добавляется в контекст:

```
<USER_MEMORY>
# User Preferences
...
</USER_MEMORY>

ORIGINAL USER REQUEST:
{user_message}
```

---

## 🗓 Scheduler

### Возможности

- **One-time reminders** — "напомни через 30 минут"
- **Recurring tasks** — каждую минуту/час/день/неделю/месяц
- **Auto-execution** — задачи с промптом автоматически запускают новый чат
- **Native notifications** — системные уведомления (macOS/Windows/Linux)

### Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                    Scheduler Service (Rust)                  │
│                                                              │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐   │
│  │ Background   │───▶│  Check due   │───▶│   Execute    │   │
│  │   Thread     │    │    tasks     │    │    task      │   │
│  └──────────────┘    └──────────────┘    └──────────────┘   │
│         │                   │                   │           │
│         ▼                   ▼                   ▼           │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                    SQLite DB                          │   │
│  │  scheduled_tasks: id, title, schedule, next_run, ...  │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### Форматы расписания

| Формат | Пример | Описание |
|--------|--------|----------|
| `in X minutes` | `in 30 minutes` | Через 30 минут |
| `in X hours` | `in 2 hours` | Через 2 часа |
| `every minute` | — | Каждую минуту |
| `every hour` | — | Каждый час |
| `every day at HH:MM` | `every day at 09:00` | Ежедневно в 9:00 |
| `every week` | — | Каждую неделю |
| `every month` | — | Каждый месяц |

### Пример использования

```
User: Напомни мне через 30 минут проверить почту

AI: Создаю напоминание...
→ schedule_task({ title: "Проверить почту", schedule: "in 30 minutes" })
```

---

## 🎨 Skills Marketplace

### Концепция

Skills — это специализированные инструкции, которые можно загружать в контекст AI для определённых задач.

### Источник

GitHub-based маркетплейс: https://vakovalskii.github.io/ValeDesk-Skills/

### Встроенные категории

| Skill | Описание |
|-------|----------|
| `algorithmic-art` | Генеративное искусство |
| `brand-guidelines` | Стиль Anthropic |
| `canvas-design` | Визуальный дизайн |
| `code-review` | Ревью кода |
| `documentation` | Написание документации |

### Кастомные маркетплейсы

Можно указать URL своего репозитория с навыками в настройках.

---

## 🔒 Безопасность

### Многоуровневая защита

| Уровень | Механизм | Описание |
|---------|----------|----------|
| **Файловая система** | Directory sandboxing | Доступ только к workspace folder |
| **JavaScript** | QuickJS WASM | Без сети, без таймеров, без модулей |
| **Python** | Subprocess | Отдельный процесс с ограничениями |
| **Инструменты** | Permission modes | Подтверждение опасных операций |
| **Данные** | Local-only | Никакой телеметрии, всё локально |

### Режимы разрешений

| Режим | Поведение |
|-------|-----------|
| `ask` | Каждый вызов инструмента требует подтверждения |
| `default` | Выполнение без запроса (для доверенных операций) |

### Sandbox ограничения

```typescript
// execute_js — QuickJS WASM
// ✅ Доступно
fs.readFileSync('/file.txt')
fs.writeFileSync('/file.txt', 'content')
JSON.parse('{"a": 1}')
console.log('Hello')

// ❌ Недоступно
require('axios')          // Error: require is not defined
await fetch('...')        // Error: fetch is not defined
setTimeout(() => {}, 100) // Error: setTimeout is not defined
```

---

## 💾 Хранение данных

### Расположение

| OS | Путь |
|----|------|
| **macOS** | `~/Library/Application Support/ValeDesk/` |
| **Windows** | `%APPDATA%/ValeDesk/` |
| **Linux** | `~/.config/ValeDesk/` |

### Файлы

| Файл | Содержимое |
|------|------------|
| `sessions.db` | SQLite: sessions, messages, todos, scheduled_tasks, settings |
| `~/.valera/memory.md` | Персистентная память пользователя |
| `~/.valera/logs/sessions/{id}/` | Логи API-запросов |

### Структура SQLite

```sql
-- Сессии чатов
CREATE TABLE sessions (
  id TEXT PRIMARY KEY,
  title TEXT,
  cwd TEXT,
  model TEXT,
  status TEXT,
  is_pinned INTEGER,
  input_tokens INTEGER,
  output_tokens INTEGER,
  created_at INTEGER,
  updated_at INTEGER
);

-- Сообщения
CREATE TABLE messages (
  id INTEGER PRIMARY KEY,
  session_id TEXT,
  type TEXT,
  data TEXT,  -- JSON
  created_at INTEGER
);

-- Todo-списки
CREATE TABLE todos (
  id INTEGER PRIMARY KEY,
  session_id TEXT,
  data TEXT  -- JSON array
);

-- Scheduled tasks
CREATE TABLE scheduled_tasks (
  id TEXT PRIMARY KEY,
  title TEXT,
  schedule TEXT,
  prompt TEXT,
  next_run INTEGER,
  is_recurring INTEGER,
  is_enabled INTEGER,
  created_at INTEGER
);

-- Настройки
CREATE TABLE settings (
  key TEXT PRIMARY KEY,
  value TEXT
);

-- LLM Providers
CREATE TABLE llm_providers (
  id TEXT PRIMARY KEY,
  name TEXT,
  type TEXT,
  base_url TEXT,
  api_key TEXT,
  enabled INTEGER,
  config TEXT
);

-- LLM Models
CREATE TABLE llm_models (
  id TEXT PRIMARY KEY,
  provider_id TEXT,
  name TEXT,
  enabled INTEGER,
  config TEXT
);
```

### Логирование API

Каждый turn логируется в `~/.valera/logs/sessions/{session-id}/`:

```
turn-001-request.json   # Запрос к LLM
turn-001-response.json  # Ответ от LLM
turn-002-request.json
turn-002-response.json
...
```

---

## 🚀 Сборка и разработка

### Требования

| Компонент | Версия |
|-----------|--------|
| **Rust** | 1.74+ |
| **Node.js** | 20+ |
| **Python** | 3.x (для `execute_python`) |

### Команды разработки

```bash
# Клонирование
git clone https://github.com/vakovalskii/ValeDesk.git
cd ValeDesk

# Установка зависимостей
npm install

# Запуск в dev-режиме (Tauri + Vite + Sidecar)
make dev

# Только UI (Vite)
make dev-ui

# Транспиляция sidecar
npm run transpile:sidecar

# Проверка типов
npm run type-check

# Линтинг
npm run lint

# Тесты
npm run test
```

### Сборка для production

```bash
# macOS (ARM64)
npm run dist:mac-arm64
# → dist/ValeDesk-X.X.X-arm64.dmg

# macOS (Intel)
npm run dist:mac-x64

# Windows
npm run dist:win
# → dist/ValeDesk Setup X.X.X.exe

# Linux
npm run dist:linux
# → dist/ValeDesk-X.X.X.AppImage
```

### Makefile цели

```makefile
make dev          # Полная разработка
make dev-ui       # Только Vite
make bundle       # Production build
make clean        # Очистка артефактов
```

---

## 📄 Лицензия

### ValeDesk Community License

**Бесплатно для:**
- Индивидуальных пользователей
- Компаний с годовой выручкой до $1,000,000 USD

**Требуется коммерческая лицензия для:**
- Компаний с выручкой свыше $1M/год
- Создания конкурирующих продуктов

### Ограничения

- Нельзя удалять copyright-уведомления
- Нельзя использовать для нарушения законов
- Нельзя создавать конкурирующие продукты

### Контакт для коммерческой лицензии

**Email:** kovalskyvr@mail.ru

---

## 📊 Диаграмма потока данных

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│    User     │────▶│   React UI  │────▶│  Tauri IPC  │
│   Input     │     │  (Zustand)  │     │   Bridge    │
└─────────────┘     └─────────────┘     └──────┬──────┘
                                               │
                                               ▼
┌─────────────────────────────────────────────────────────────┐
│                      Rust Backend                            │
│                                                              │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐   │
│  │   main.rs    │───▶│   db.rs      │    │  scheduler   │   │
│  │  IPC Router  │    │   SQLite     │    │    .rs       │   │
│  └──────┬───────┘    └──────────────┘    └──────────────┘   │
│         │                                                    │
│         │ stdin/stdout (JSON)                               │
│         ▼                                                    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              Node.js Sidecar                         │    │
│  │                                                      │    │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐     │    │
│  │  │   runner   │  │   tools    │  │  session   │     │    │
│  │  │  openai.ts │  │ executor   │  │   store    │     │    │
│  │  └─────┬──────┘  └─────┬──────┘  └────────────┘     │    │
│  │        │               │                             │    │
│  │        ▼               ▼                             │    │
│  │  ┌─────────────────────────────────────────────┐    │    │
│  │  │              LLM API (OpenAI SDK)            │    │    │
│  │  │   vLLM / Ollama / LM Studio / OpenAI        │    │    │
│  │  └─────────────────────────────────────────────┘    │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                     External Services                        │
│                                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │
│  │  Tavily  │  │   Z.AI   │  │ Playwright│  │   Git    │     │
│  │  Search  │  │  Reader  │  │  Browser  │  │  Repos   │     │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘     │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔑 Ключевые файлы для изучения

| Файл | Что изучить |
|------|-------------|
| `src/agent/libs/runner-openai.ts` | LLM loop, streaming, retry logic |
| `src/agent/libs/prompts/system.txt` | Системный промпт AI |
| `src/agent/libs/tools-executor.ts` | Выполнение инструментов |
| `src-tauri/src/main.rs` | IPC, sidecar management |
| `src-tauri/src/db.rs` | SQLite операции |
| `src/ui/store/useAppStore.ts` | Frontend state management |

---

## 🎯 Roadmap (неофициальный)

На основе структуры проекта можно предположить:

- [ ] Улучшение browser automation
- [ ] Больше LLM-провайдеров (Anthropic native, Google Gemini)
- [ ] Плагинная система для инструментов
- [ ] Улучшенный UI для code editing
- [ ] Multi-agent capabilities
- [ ] Voice input/output

---

<div align="center">

**ValeDesk v0.0.8**

Made with ❤️ by [Valerii Kovalskii](https://github.com/vakovalskii)

</div>